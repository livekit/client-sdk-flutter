class Constants {
  static const defaultConnectionTimeout = Duration(seconds: 5);
  static const defaultDebounceTimeout = Duration(milliseconds: 100);
  static const defaultPublishTimeout = Duration(seconds: 3);
  static const defaultIceConnectionTimeout = Duration(seconds: 5);
  static const defaultIceRestartTimeout = Duration(seconds: 10);
}
