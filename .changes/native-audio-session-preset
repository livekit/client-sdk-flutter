patch type="fixed" "iOS: in automatic mode the audio session is now configured from engine state even before any policy is pushed — capture that starts before connect (pre-connect audio, pre-join mic) gets a recording session, fixing audio engine error -9001, and playout-only starts get a playback session"
